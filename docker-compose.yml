version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=/api
      - VITE_API_USERNAME=user
      - VITE_API_PASSWORD=2997dc0b00851972aa1b6d6f2628478a
      - VITE_FRONTEND_URL=https://minpaku-sales-app-tunnel-v00h1col.devinapps.com
      - VITE_FRONTEND_USERNAME=user
      - VITE_FRONTEND_PASSWORD=dc41a724150fb9e9b52896e635f4fd82
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - SECRET_KEY=your-secret-key-for-development
      - LINE_CHANNEL_ACCESS_TOKEN=${LINE_CHANNEL_ACCESS_TOKEN}
      - USE_FIRESTORE=false
